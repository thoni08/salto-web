# Thread Detail API mode
# mock: frontend-only (default fallback)
# live: call backend API
VITE_THREAD_DETAIL_API_MODE=mock

# Required if mode=live
# Example: http://localhost:3000/api
VITE_API_BASE_URL=
